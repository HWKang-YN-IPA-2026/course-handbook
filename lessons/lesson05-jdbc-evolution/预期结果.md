# 第05次课JDBC实验预期结果

## 数据库准备

`prepare_database.sh lesson05_demo`从第04次课v0.3基线继续迁移，预期出现：

```text
AC-401 PASS: v0.4 history columns exist
AC-402 PASS: passed and failed histories are reproducible
AC-403 PASS: one-seat concurrency fixture exists
数据库 lesson05_demo 已重建为选课系统 v0.4。
```

## Course0与classpath

不包含`target/dependency/*`时：

```text
ClassNotFoundException: org.postgresql.Driver
EXPECTED_FAIL exit=1
```

加入pgJDBC后：

```text
DRIVER_OK org.postgresql.Driver
JAR=.../target/dependency/postgresql-42.7.13.jar
```

第一段错误是预期教学结果，演示脚本会继续执行。

## Course1参数化连接查询

输入学生编号`S002`后：

```text
student_id | student_name | course_name | term_name | score | passed
S002 | 李同学 | 高等数学 | 2025-2026学年春季学期 | 45.00 | false
ROWS=1
```

## 教师业务演示

```text
01 已通过者再次修读 -> REJECT / RETAKE_NOT_ALLOWED
02 未通过者重修 -> PASS / ENROLLED
03 过期协议的外校生 -> REJECT / NO_ACTIVE_AGREEMENT
04 两个请求同时竞争最后一个座位
预期：成功 1 人；实际：成功 1 人
```

并发胜者可能是C001或C002，另一人必须收到`CLASS_FULL`。

## 自动验收

```text
AC-404 PASS: RETAKE_NOT_ALLOWED
AC-405 PASS: ENROLLED
AC-406 PASS: DUPLICATE_TERM_COURSE
AC-407 PASS: NO_ACTIVE_AGREEMENT
AC-408 PASS: ENROLLED
AC-409 PASS: EXTERNAL_QUOTA_FULL
AC-410 PASS: two concurrent requests produce one winner
AC-411 PASS: database contains exactly one concurrent winner
ALL LESSON-05 CHECKS PASSED
```

## 错误实验

`database/05_join_error_lab.sql`按段执行：

- 遗漏连接条件时，结果行数异常增加；
- 查询不存在的`e.score`时，PostgreSQL报告SQLSTATE `42703`；
- 定义别名`s`却引用`student`时，PostgreSQL报告缺少FROM子句项。

该文件故意保留错误，不能加入自动数据库准备脚本。
