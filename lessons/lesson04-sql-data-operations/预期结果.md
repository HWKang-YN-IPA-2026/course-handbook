# 第04次课SQL实验预期结果

## SELECT、排序与聚合

- 开放教学班为`MATH101-01`和`MATH101-02`。
- 容量降序为40、35。
- `class_count=2`、`min_capacity=35`、`max_capacity=40`、`avg_capacity=37.5`。
- 按课程分组时，`MATH101`有2个教学班。

## INSERT与约束

- 事务内新增S002后选课记录数为2。
- ROLLBACK后恢复为1。
- S001在同一学期再次选择MATH101时出现预期唯一约束错误。

## UPDATE、DELETE与恢复

- 定向UPDATE影响1行，回滚后PARTNER配额仍为1。
- 临时学生TMP01删除后计数为0，事务结束后基线学生仍为5人。
- 无WHERE UPDATE会临时把两条协议配额都改为0；回滚后PARTNER为1、OTHER为2。

## 自动验收

`verify_postgres.sh`末尾输出AC-301至AC-306全部PASS。
